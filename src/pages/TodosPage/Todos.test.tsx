// check the existence of input
import React from 'react';
import { configure } from 'enzyme';
import Adapter from 'enzyme-adapter-react-16';

describe('Todos', () => {
  it('Todos should contain one input', () => {});
});
