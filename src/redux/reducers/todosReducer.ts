const initialState: any = {
  todos: [],
  loading: true,
};

const todoReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default todoReducer;
